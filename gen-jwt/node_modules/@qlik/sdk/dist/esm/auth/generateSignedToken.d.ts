/// <reference types="node" />
import { ClaimsAndOptions } from '../types/types.js';
/**
 * method helper for easily creating signedTokens (backends/serverless functions)
 */
export default function generateSignedToken({ jti, sub, subType, name, email, email_verified, groups, expiresIn, notBefore, keyid, issuer, algorithm, audience, }: ClaimsAndOptions, crt: string | Buffer): string;
